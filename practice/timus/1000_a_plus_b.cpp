#include <bits/stdc++.h>

#ifdef COMPETITIVE_DEBUG
#include "test_runner.h"
#else
#define RUN_WITH_TESTS()
#endif

int main()
{
    RUN_WITH_TESTS()
    
    uint64_t a{}, b{};
    std::cin >> a >> b;
    std::cout << a + b << std::endl;

    return 0;
}